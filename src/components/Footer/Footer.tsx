// CSS Import
import classes from './Footer.module.css';

const Footer = (props: unknown) => {
	return (
		<footer className={classes.footer}>
			<h6 className={classes.h6}>Footer Content</h6>
		</footer>
	);
}

export default Footer;